# For local development, VITE_API_BASE_URL is generally not needed as Vite proxies '/api' to the backend.
# In production (e.g., Render), set this to your backend service URL.
VITE_API_BASE_URL=https://api.your-backend.onrender.com/api
